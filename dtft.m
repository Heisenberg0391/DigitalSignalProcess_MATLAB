close all
clc
N=16;                         %原离散信号有8点
n=[0:1:N-1];                  %原信号是1行8列的矩阵
xn=0.5.^n;                   %构建原始信号，为指数信号

w=[-800:1:800]*2*pi/800;     %频域共-2π----+2π 的长度（本应是无穷，高频分量很少，故省去）    
X=xn*exp(-1i*(n'*w));         %求dtft变换，采用原始定义的方法，对复指数分量求和而得
subplot(2,1,1);
stem(n,xn);
title('原始信号(指数信号)');
subplot(2,1,2);
plot(w/pi,abs(X));
title('DTFT变换')
